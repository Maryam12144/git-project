name: learn yaml for devops

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  yaml-test:
    runs-on: ubuntu-latest
    steps:
     - name: Print-hello
       run: echo "Hello From the Other Side!"


